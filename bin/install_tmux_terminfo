#!/usr/bin/env bash

mkdir "$HOME"/.terminfo

cat <<EOF > "$HOME"/.terminfo/tmux.terminfo
tmux|tmux,
  ritm=\E[23m, rmso=\E[27m, sitm=\E[3m, smso=\E[7m, Ms@,
  use=xterm, use=screen,

tmux-256color|tmux with 256 colors,
  use=xterm-256color, use=tmux,
EOF

tic -x "$HOME"/.terminfo/tmux.terminfo
