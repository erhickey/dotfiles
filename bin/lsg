#!/usr/bin/env bash

if [ $# -eq 0 ] ; then
    dir=.
else
    dir="$1"
fi

case "$(uname -a)" in

    *Microsoft*)
        explorer.exe "$dir"
        ;;

    *Darwin*)
        open "$dir"
        ;;

    *)
        xdg-open "$dir"
        ;;

esac
