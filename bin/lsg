#!/usr/bin/env bash

if [ $# -eq 0 ] ; then
    dir=.
else
    dir="$1"
fi

case "$(uname -r)" in

    *Microsoft*)
        explorer.exe "$dir"
        ;;

    *)
        xdg-open "$dir"
        ;;

esac
